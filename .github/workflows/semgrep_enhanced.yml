name: Semgrep Security Scan

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read
  pull-requests: write
  security-events: write
  actions: read

jobs:
  semgrep:
    name: Run Semgrep Security Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    
    if: (github.actor != 'dependabot[bot]')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      # PR인 경우 변경된 파일 목록 가져오기
      - name: Get changed files (PR only)
        id: changed-files
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prFiles = await github.rest.pulls.listFiles({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              per_page: 100
            });
            
            // 스캔 가능한 파일 확장자 필터링 (삭제된 파일 제외)
            const scanExtensions = ['.py', '.js', '.ts', '.jsx', '.tsx', '.java', '.go', '.rb', '.php', '.c', '.cpp', '.h', '.cs', '.swift', '.kt', '.scala', '.rs'];
            
            const changedFiles = prFiles.data
              .filter(file => file.status !== 'removed')
              .filter(file => scanExtensions.some(ext => file.filename.endsWith(ext)))
              .map(file => file.filename);
            
            console.log('변경된 스캔 대상 파일:', changedFiles);
            
            if (changedFiles.length === 0) {
              console.log('스캔할 파일이 없습니다.');
              core.setOutput('has_files', 'false');
              core.setOutput('files', '');
            } else {
              core.setOutput('has_files', 'true');
              // 파일 목록을 공백으로 구분하여 저장
              core.setOutput('files', changedFiles.join(' '));
            }
            
            return changedFiles;
      
      # Push인 경우: 전체 스캔
      - name: Run Semgrep Full Scan (Push)
        if: github.event_name == 'push'
        run: |
          echo "🔍 Push 이벤트: 전체 코드베이스 스캔"
          semgrep scan \
            --config p/security-audit \
            --sarif \
            --output=semgrep.sarif \
            --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # PR인 경우: 변경된 파일만 스캔
      - name: Run Semgrep Diff Scan (PR)
        if: github.event_name == 'pull_request' && steps.changed-files.outputs.has_files == 'true'
        run: |
          echo "🔍 PR 이벤트: 변경된 파일만 스캔"
          echo "스캔 대상 파일: ${{ steps.changed-files.outputs.files }}"
          semgrep scan \
            --config p/security-audit \
            --sarif \
            --output=semgrep.sarif \
            --verbose \
            ${{ steps.changed-files.outputs.files }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # PR에서 스캔할 파일이 없는 경우 빈 SARIF 생성
      - name: Create empty SARIF (No scannable files)
        if: github.event_name == 'pull_request' && steps.changed-files.outputs.has_files == 'false'
        run: |
          echo "스캔할 파일이 없습니다. 빈 SARIF 파일 생성."
          echo '{"version":"2.1.0","$schema":"https://json.schemastore.org/sarif-2.1.0.json","runs":[{"tool":{"driver":{"name":"Semgrep","semanticVersion":"1.0.0","rules":[]}},"results":[]}]}' > semgrep.sarif
      
      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: semgrep.sarif
      
      - name: Enhanced PR Comment with Code Snippets
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // PR 요약 코멘트 (Conversation 탭)와 인라인 리뷰 코멘트 (Files changed 탭) 모두 생성
            const fs = require('fs');
            
            // SARIF 파일 읽기
            let sarif;
            try {
              sarif = JSON.parse(fs.readFileSync('semgrep.sarif', 'utf8'));
            } catch (error) {
              console.log('SARIF 파일을 찾을 수 없습니다:', error);
              return;
            }
            
            const results = sarif.runs?.[0]?.results || [];
            const rules = sarif.runs?.[0]?.tool?.driver?.rules || [];
            
            if (results.length === 0) {
              const comment = '## ✅ Semgrep 스캔 완료\n\n변경된 파일에서 보안 이슈가 발견되지 않았습니다.';
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment
              });
              return;
            }
            
            // 규칙 정보를 Map으로 구성
            const ruleMap = new Map();
            rules.forEach(rule => {
              ruleMap.set(rule.id, rule);
            });
            
            // 심각도별로 그룹화
            const severityMap = {
              critical: { emoji: '🔴', label: 'Critical', items: [] },
              high: { emoji: '🟠', label: 'High', items: [] },
              medium: { emoji: '🟡', label: 'Medium', items: [] },
              low: { emoji: '🟢', label: 'Low', items: [] },
              info: { emoji: 'ℹ️', label: 'Info', items: [] }
            };
            
            // 중복 제거를 위한 Set
            const seenIssues = new Set();
            
            results.forEach(result => {
              const location = result.locations?.[0]?.physicalLocation;
              if (!location) return;
              
              const ruleId = result.ruleId || 'unknown';
              const path = location.artifactLocation?.uri || 'unknown';
              const startLine = location.region?.startLine || 0;
              const endLine = location.region?.endLine || startLine;
              
              // 중복 체크 (같은 파일, 같은 라인, 같은 규칙은 스킵)
              const issueKey = `${ruleId}:${path}:${startLine}`;
              if (seenIssues.has(issueKey)) {
                return;
              }
              seenIssues.add(issueKey);
              
              const message = result.message?.text || 'No message';
              const snippet = location.region?.snippet?.text || '';
              
              // 규칙 정보 가져오기
              const rule = ruleMap.get(ruleId);
              const ruleDescription = rule?.shortDescription?.text || 
                                     rule?.fullDescription?.text || 
                                     message;
              
              // 심각도 결정 (우선순위: level > properties.security-severity > defaultConfiguration.level)
              let severity = 'info';
              
              // 1. result.level 확인
              const level = result.level?.toLowerCase();
              if (level === 'error') {
                severity = 'critical';
              } else if (level === 'warning') {
                severity = 'medium';
              } else if (level === 'note') {
                severity = 'low';
              }
              
              // 2. rule의 security-severity 확인 (더 정확함)
              if (rule?.properties?.['security-severity']) {
                const secSeverity = rule.properties['security-severity'];
                if (typeof secSeverity === 'string') {
                  severity = secSeverity.toLowerCase();
                } else if (typeof secSeverity === 'number') {
                  // CVSS 점수 기반 변환
                  if (secSeverity >= 9.0) severity = 'critical';
                  else if (secSeverity >= 7.0) severity = 'high';
                  else if (secSeverity >= 4.0) severity = 'medium';
                  else if (secSeverity >= 0.1) severity = 'low';
                  else severity = 'info';
                }
              }
              
              // 3. rule의 defaultConfiguration.level 확인
              if (!severity || severity === 'info') {
                const defaultLevel = rule?.defaultConfiguration?.level?.toLowerCase();
                if (defaultLevel === 'error') severity = 'critical';
                else if (defaultLevel === 'warning') severity = 'medium';
                else if (defaultLevel === 'note') severity = 'low';
              }
              
              // 4. 규칙 이름으로 추론 (fallback)
              if (severity === 'info') {
                const lowerRuleId = ruleId.toLowerCase();
                if (lowerRuleId.includes('injection') || 
                    lowerRuleId.includes('deserialization') ||
                    lowerRuleId.includes('sql') ||
                    lowerRuleId.includes('command')) {
                  severity = 'critical';
                } else if (lowerRuleId.includes('security')) {
                  severity = 'high';
                }
              }
              
              // 심각도가 없으면 info로 처리
              if (!severityMap[severity]) {
                severity = 'info';
              }
              
              severityMap[severity].items.push({
                path,
                startLine,
                endLine,
                message,
                ruleId,
                snippet,
                description: ruleDescription,
                severity
              });
            });
            
            // 통계 계산
            const totalCritical = severityMap.critical.items.length;
            const totalHigh = severityMap.high.items.length;
            const totalMedium = severityMap.medium.items.length;
            const totalLow = severityMap.low.items.length;
            const totalInfo = severityMap.info.items.length;
            const total = totalCritical + totalHigh + totalMedium + totalLow + totalInfo;
            
            // Summary 코멘트 생성
            let comment = '## 🔍 Semgrep Security Scan Results\n\n';
            comment += '> 📂 **Scanned**: Changed files in this PR only\n\n';
            comment += '### 📊 Summary\n\n';
            comment += '| Severity | Count |\n';
            comment += '|----------|-------|\n';
            comment += `| 🔴 **Critical** | **${totalCritical}** |\n`;
            comment += `| 🟠 **High** | **${totalHigh}** |\n`;
            comment += `| 🟡 Medium | ${totalMedium} |\n`;
            comment += `| 🟢 Low | ${totalLow} |\n`;
            comment += `| ℹ️ Info | ${totalInfo} |\n`;
            comment += `| **Total** | **${total}** |\n\n`;
            
            if (totalCritical > 0 || totalHigh > 0) {
              comment += '> ⚠️ **Action Required**: Critical or High severity issues found. Please review and fix them before merging.\n\n';
            } else if (total > 0) {
              comment += '> ℹ️ Issues found. Please review the details below.\n\n';
            }
            
            comment += '---\n\n';
            comment += '### 📝 Detailed Findings\n\n';
            comment += '**All security findings are available as inline review comments in the "Files changed" tab.**\n\n';
            comment += 'Each issue includes:\n';
            comment += '- 🎯 Exact location in your code\n';
            comment += '- 📖 Detailed explanation of the vulnerability\n';
            comment += '- 💡 Recommendations for fixing\n';
            comment += '- 🔗 Links to documentation\n\n';
            comment += '👉 **[Go to Files changed tab to see all inline comments](';
            comment += `https://github.com/${context.repo.owner}/${context.repo.repo}/pull/${context.issue.number}/files)**\n\n`;
            comment += '---\n\n';
            comment += '### 📚 Additional Resources\n\n';
            comment += `- [View all findings in Security tab](https://github.com/${context.repo.owner}/${context.repo.repo}/security/code-scanning)\n`;
            comment += `- [Semgrep Rule Documentation](https://semgrep.dev/r)\n`;
            comment += `- [Semgrep Playground](https://semgrep.dev/playground) - Test rules interactively\n`;
            comment += '\n> 💡 **Tip**: Click "Resolve conversation" on each inline comment after fixing the issue.\n';
            
            // 코멘트 길이 제한
            if (comment.length > 65000) {
              comment = comment.substring(0, 65000) + '\n\n... (truncated due to length)';
            }
            
            // 1. PR 요약 코멘트 생성 (Conversation 탭)
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });
            
            // 2. 인라인 리뷰 코멘트 생성 (Files changed 탭에 Reply 버튼 포함)
            // Critical/High 이슈만 인라인 코멘트로 추가
            const criticalAndHighIssues = [
              ...severityMap.critical.items,
              ...severityMap.high.items
            ];
            
            if (criticalAndHighIssues.length > 0) {
              // PR의 HEAD SHA 가져오기
              const pr = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number
              });
              
              const headSha = pr.data.head.sha;
              
              // Review 생성 (인라인 코멘트를 그룹화)
              // 변경된 파일만 스캔했으므로 모든 결과가 PR diff에 포함됨
              const reviewComments = criticalAndHighIssues.slice(0, 10).map(item => {
                // path 정규화 (앞에 붙은 ./ 또는 / 제거)
                const normalizedPath = item.path.replace(/^\.?\//, '');
                
                // 등급별 이모지와 텍스트 매핑
                const severityDisplayMap = {
                  'critical': { emoji: '🔴', text: 'Critical' },
                  'high':     { emoji: '🟠', text: 'High' },
                  'medium':   { emoji: '🟡', text: 'Medium' },
                  'low':      { emoji: '🟢', text: 'Low' },
                  'info':     { emoji: 'ℹ️', text: 'Info' }
                };

                // 매핑 정보가 없을 경우를 대비한 기본값 설정
                const { emoji, text } = severityDisplayMap[item.severity.toLowerCase()] || { emoji: '⚪', text: item.severity };

                let body = `## ${emoji} ${text}: ${item.ruleId}\n\n`;
              
                body += `**${item.description}**\n\n`;
                body += `💬 ${item.message}\n\n`;
                body += `[Learn more about this rule](https://semgrep.dev/r/${item.ruleId})`;
                
                return {
                  path: normalizedPath,
                  line: item.startLine,
                  body: body
                };
              });
              
              try {
                await github.rest.pulls.createReview({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: context.issue.number,
                  commit_id: headSha,
                  event: 'COMMENT',
                  comments: reviewComments
                });
                console.log(`${reviewComments.length}개의 인라인 리뷰 코멘트 생성 완료`);
              } catch (error) {
                console.log('인라인 리뷰 코멘트 생성 실패:', error.message);
                // 개별 코멘트 시도 (일부만 실패할 경우를 위해)
                for (const comment of reviewComments) {
                  try {
                    await github.rest.pulls.createReviewComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      pull_number: context.issue.number,
                      commit_id: headSha,
                      path: comment.path,
                      line: comment.line,
                      body: comment.body
                    });
                    console.log(`개별 코멘트 생성 성공: ${comment.path}:${comment.line}`);
                  } catch (individualError) {
                    console.log(`개별 코멘트 생성 실패 (${comment.path}:${comment.line}):`, individualError.message);
                  }
                }
              }
            }
